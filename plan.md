A task Manager User creates Projects inside then live Tasks.

Projects can be added the name can be changed and they can be Deleted.

Tasks live inside Projects when A Project is clicked here the Task Management happens 

Tasks can be added edited deleted and set as Done

Data Structure:
Projects:
[{id, projectName}]
[{id, taskName, project, isDone}]

1. Add Project
    - set Projects Array
    - set Project Name
    - add input
    - add p that changes when input changes
    - call the function in a button